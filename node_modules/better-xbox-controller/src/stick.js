// Generated by CoffeeScript 1.10.0
(function() {
  var XBoxStick,
    bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  XBoxStick = (function() {
    function XBoxStick(x, y) {
      this.x = x;
      this.y = y;
      this.isEqual = bind(this.isEqual, this);
      this.getAngle = bind(this.getAngle, this);
      this.getLength = bind(this.getLength, this);
    }

    XBoxStick.prototype.getLength = function() {
      return Math.sqrt(this.x * this.x + this.y * this.y);
    };

    XBoxStick.prototype.getAngle = function() {
      var d;
      d = Math.atan2(this.y, this.x);
      return (d > 0 ? d : 2 * Math.PI + d) * 360 / (2 * Math.PI);
    };

    XBoxStick.prototype.isEqual = function(stick) {
      if (stick instanceof XBoxStick) {
        if (stick.x === this.x && stick.y === this.y) {
          return true;
        }
      }
      return false;
    };

    return XBoxStick;

  })();

  module.exports = XBoxStick;

}).call(this);
